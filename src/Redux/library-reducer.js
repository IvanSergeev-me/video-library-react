

let initialState = {
    videos:[]
};
const libraryReducer = (state = initialState, action) => {
    
    switch (action.type) {

        default: return state;
    };
};


export default libraryReducer;
